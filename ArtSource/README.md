# 制作用の元データ
Unity に直接取り込まない Blender・Photoshop 等の元データを置きます。
`Features/<FeatureName>/<AssetName>/` または `Common/<AssetName>/` に整理し、Unity 用の書き出し先は `Assets/_Project/Content` の対応する場所にします。
フォルダは元データ追加時に作成してください。Unity のインポート対象外なので .meta は不要です。
